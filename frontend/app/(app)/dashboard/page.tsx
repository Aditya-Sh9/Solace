export default function DashboardPage() {
  return (
    <div style={{ maxWidth: 560, margin: '64px auto 0', textAlign: 'center' }}>
      <p className="serif" style={{ fontSize: 22, color: 'var(--ink-soft)', margin: 0 }}>
        Nothing here yet — which is its own kind of start.
      </p>
      <p style={{ marginTop: 12, color: 'var(--ink-muted)', fontSize: 15 }}>
        Whenever you're ready.
      </p>
    </div>
  )
}
